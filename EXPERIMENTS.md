# Experiment log — overview

One file per experiment in `experiments/`; this file is the index. The
conceptual framework is in `README.md`, the working norms in `AGENTS.md`,
and the named mathematical objects, the quantity each experiment
estimates, the proposal/abstraction/interpreter separation, the invariance
principle, and the verdict-predicate taxonomy are in `FORMALISM.md`.

| # | file | script(s) | question | verdict | status |
|---|---|---|---|---|---|
| 1 | [1-sufficiency.md](experiments/1-sufficiency.md) | analysis.py, refine.py | does completeness saturate at the belief dimension; can CEGAR discover it? | yes on Z1R (k=1, the *reachable* belief set); on Mess3 the top-k PCA subspace misses the belief plane — proposal misalignment vs curvature left open | concluded |
| 2 | [2-proposal-families.md](experiments/2-proposal-families.md) | compare.py | misalignment or curvature? | misalignment: X-whitened PLS k\*=2 hits the full-residual identification R² (0.9916 vs 0.9917); seed-stable | concluded |
| 3 | [3-readout-interventions.md](experiments/3-readout-interventions.md) | intervene.py | is the discovered subspace causally load-bearing at the readout? | no — correlational-but-not-causal (closure 63% / 0.7%); the causal channel is the LN-linearized unembedding pullback (100.0% closure at k=V, validated empirically) | concluded |
| 4 | [4-midstream-interventions.md](experiments/4-midstream-interventions.md) | midstream.py | do interventions persist mid-stream, through attention, over multi-token horizons? coherence as state? | P1–P5 hold, P6 fails: per-step incremental closure 12.5%/0.0% at steps 2/3 — future positions re-derive state from raw tokens below the patch; the stream is a per-position *summary*, not propagated *state* | concluded |
| 5 | [5-depth-profile.md](experiments/5-depth-profile.md) | depth.py | at which depth, if any, does the stream function as state? (4-layer Mess3, patch every interior layer, per-step incremental closure profile) | P1–P6 all hold: state early (L1: 93.7%/91.0% incremental, 94.8% coherence), summary late — and at L3 incremental closure goes *negative* (−29.7%/−83.7%): mixed-provenance state corrupts (new typed failure: state interference) | concluded |
| 6 | [6-interventional-discovery.md](experiments/6-interventional-discovery.md) | discover.py | can a CEGAR loop scored by *observable* interchange closure at L1 discover a low-dim causal abstraction approaching full-patch closure, avoiding the PLS echo failure — without trivially growing k? | P1–P7 all hold: k\*=2 (= belief-simplex dim) at 98.3% vs full 98.7%; echo avoided (pls 2.7%, near-orthogonal); oracle-free score tracks exact closure to 1.5 points; converged to the variance plane (declared mimicry limit → experiment 8 is the discriminator) | concluded |
| 7 | [7-dyck.md](experiments/7-dyck.md) | dyck.py (+ processes.dyck2) | does the battery transfer to a stack process (depth-bounded Dyck-2, 15 states)? state-vs-bypass tension: attention can bracket-match from raw tokens | P3–P9 hold, P1–P2 fail *informatively*: the linear-belief calibration breaks (R² 0.66, decode k\*>12, k_B=13) on a behaviorally-exact model, while interventional CEGAR finds a 4-dim causal core closing 92.6% vs full 93.6% with oracle-free scoring sound (5.9 pts) — new typed outcome: representation–oracle mismatch; no state interference on Dyck; variance mimicry recurs (→ exp 8 still needed) | concluded |
| 8 | [8-adversarial-coordinates.md](experiments/8-adversarial-coordinates.md) | adversarial.py | does interventional discovery survive adversarially ill-conditioned coordinates where PCA must fail? (closes exp 6/7's variance-mimicry limitation; constructed validation, exp-2 buried-cache tradition) | P1/P4/P5 hold, P2/P3 fail: VARIANCE DEPENDENCE EXPOSED — the loop's second-moment *proposal* miner chases amplified junk and dies at k\*=0, while behavioral *acceptance* produced no false confidence (rejected the first junk proposal and stopped; reported its own null result consistently). Exp 6/7's discoveries were proposal-variance-lucky; the scale-free miner repair is the next registration. Surprises: pls-z flips echo→85% causal; rand-z is destructive (−427%, pullback off-manifold amplification) | concluded |
| 9 | [9-scale-free-mining.md](experiments/9-scale-free-mining.md) | miners.py | can the proposal map be made scale-free with acceptance unchanged? M1 covariance (anchors) vs M2 whitened (the invariance repair, FORMALISM.md §5 proposition asserted numerically) vs M3 centered-weight (decorrelation, ~50% credence), both regimes; completes exp-8's P4 matrix | P1/P7 hold; P2/P3/P6 fail; P4 NOT TESTED; P5 three-way: probe HOLDS (invariance confirmed at machine precision; κ-fingerprint), accepted-k clause NOT TESTED. The repair fails anyway — **in both regimes**: M2's direction is near-causal (14.9°), not echo (85.9°), but selection-blurred (spectrum λ1/mean 37→13) with weak oblique coupling — behaviorally null. Variance was the selection signal (M1's success = luck, triply confirmed); M3 works benignly (95.5%) but dies adversarially. Correlational mining exhausted; proposals must become interventional → exp 10 | concluded |
| 10 | [10-interventional-search.md](experiments/10-interventional-search.md) | candidates.py | finite-candidate interventional search: 12-candidate pools from cheap fallible sources (incl. the exp-9 losers), ranked by *measured closure gain under patching*, acceptance unchanged, uniform working-coordinate patch convention judged after pullback; adjudicates selection-works / support-or-patch-failure / mere-reranking via registered per-candidate gain+angle tables; gradients deferred to exp 11 | P1/P2/P6 hold; P3 fails; P4/P5 NOT TESTED. Trichotomy adjudicates to **2b: patch-parameterization failure** (registered 2a/2b wording overlapped; ambiguity recorded, resolved on the intended geometric boundary) — the adversarial pool reached the plane (nearest candidate 1.1° at +1.0% gain, the exp-9 loser's other back-mapping; best gain +1.5% at 4.1°) with near-plane randoms catastrophically destructive (−105/−187%): the working-coordinate orthogonal swap's *read side* is junk-amplified ×κ (needs contamination ≲ κ⁻² to behave). Benignly, selection works and is not reranking (dPCA1 beat M1 by measured gain; k\*=2 at 98.4%). The exp-3 interchange patch is the remaining coordinate-lucky component; patches must be co-selected with directions → exp 11, with gradients explicitly warned | concluded |
| 11 | [11-patch-parameterization.md](experiments/11-patch-parameterization.md) | patches.py | the candidate becomes a (write direction, read covector) pair — patch maps join the search space; read families id/prec/cov per write (prec = the GLS read, provably coordinate-equivariant — the P3 mechanism candidate at ~65%); oblique composition with conditioning guard; diagnostic anchors (known-plane ceiling; stream-orthogonal patch of the nearest write = direct P6 verification of exp-10's read-side mechanism); first registration audited against FORMALISM §6.1, all P's with NOT TESTED residuals | P1/P2/P6/P7 hold; P3 fails; P4/P5 NOT TESTED (P4: 4th consecutive). Partition → **(ii) read-menu insufficiency, read-concept vindicated**: same 1.1° write earns +1.0% (id) vs **+51.3%** (stream-clean diagnostic) — exp-10's mechanism verified by controlled intervention; branch (iii) (linear primitive broken) refuted. `prec` does half the repair: destruction eliminated (cov −606% → prec +1.4% on the same write; high read-junk necessary for destruction, not sufficient) but under-transfers *everywhere* (benign: +16.1% vs id's +54.3% on the same 0° write, read-junk 3% vs 0% — GLS controls away signal); best adversarial gain +4.5%, 0.5 pts under eps_gain. Equivariance isolation confounded by regime-dependent writes (flagged). → exp 12: read covector joins the search | concluded |
| 12 | [12-read-search.md](experiments/12-read-search.md) | reads.py | the read covector becomes a one-parameter *search*: c_α ∝ Σ̂^{−α}w, α ∈ {0,…,1} (nests the id and prec failures), write-pool × grid ranked by measured gain, eps_gain frozen at 0.05 against the +4.5% temptation. Registered **impossibility note**: no α equals the clean read (κ-cancellation needs α=1, spectrum-cleanliness needs α=0) — success means "good-enough tradeoff", never "clean read constructed". D2 anchor closes the composition escape hatch in-run; same-write equivariance table fixes exp-11's confound | P1/P2/P6/P7 hold; P3 fails; P4/P5 NOT TESTED (P4: 5th). Branch (ii): **no Σ̂-spectral read suffices** — the same-write equivariance table shows adversarial gains flat ~+1.5% across the whole α-grid (no interior optimum), equivariance and transfer anti-correlated, prec exactly equivariant (+1.4% = +1.4% — exp-11's "ridge gap" was write-difference, worry retired). Mechanism: low read-junk demonstrably does not buy transfer (4–7% junk on the nearest write, no gain; most near-plane writes shed junk only at α=1); *leading hypothesis* (inferred, not yet decomposed): neutral-background read contamination → exp-13 registers the decomposition diagnostic. **D2 composition exonerated at 97.8%** (two clean-read patches at 1.1°+3.3° stack to the ceiling): the program's whole adversarial gap is now one object — an honest clean-read construction → exp 13 (paired pools / read refinement / read-only gradient, now properly scoped) | concluded |
| 13 | [13-read-gradient.md](experiments/13-read-gradient.md) | readopt.py | the read covector is *learned from behavioral signal*: fixed writes (the exp-12 near-plane pair, reproduced in-run), Adam on the observable model-vs-model KL through the differentiable exact chain, ⟨c,w⟩=1 renormalized; dual inits; read-decomposition diagnostic turns exp-12's neutral hypothesis into measurement (P6); **P4 dual reading registered**: agreement under maximal selection pressure = strongest oracle-free soundness datum, divergence = objective hacking located — both first-class | P1/P2/P4/P6a/P7 hold; P3 fails; P5/P6b NOT TESTED. **P4 HOLDS at last** (6th experiment): observable tracks exact to 0.3/1.6 pts on gradient-optimized adversarial patches — oracle-free scoring survives maximal selection pressure, no objective hacking. **Discovery:** w2's learned read transfers +43.7% with **zero plane mass** (49% junk/51% neutral) — the clean-read geometric picture is too narrow; new hypothesis: reads are *statistical predictors* exploiting echo correlations. P3's failure: w1 runs diverged to 100%-junk reads (−498%) while w2 converged through identical machinery — an optimization-dynamics failure, not unlearnability; leading mechanism *consistent-with, not proven* (printed losses are per-minibatch): renormalization feedback — settling diagnostics + affine-slice repair registered for the follow-up. P6a at 100%: spectral inits are pure-neutral, exp-12's hypothesis confirmed as measurement | concluded |
| 14 | [14-affine-reads.md](experiments/14-affine-reads.md) | readaffine.py | four-arm discriminator for exp-13's open objects: (A) instrumented rerun of the w1 divergence under the *original* renormalized parameterization — a registered three-part feedback signature (full-objective ascent, ⟨c,w⟩ shrinkage bias, norm/junk runaway) upgrades the mechanism to *measured* or demotes it; (B) the affine-slice repair c = c₀ + (I−ŵŵᵀ)u, ⟨c,w⟩=1 by construction, no post-step ops; (C) learned composition vs the D2 ceiling (gated); (D) effective-plane-reading score EPR = corr²(Δ·r, Δ·u_clean) on held-out eval deltas — makes the statistical-read hypothesis measured. P6 direction explicitly flipped from exp-13 (plane ≤ 20% now expected). Single-T and fixed-write indexing inherited deliberately (the generality sweep = exp-15 candidate) | pre-registered P1–P8 (P3 headline ~55%; affine-slice + EPR ledger rows added) | pre-registered, not run |

## Conventions

KL in nats, held-out unless stated; "affine R²" = held-out R² of an affine
map fit on the train split. Honesty constraint everywhere: proposal families
and probes are supervised on observables (completions) only — belief states
are evaluation ground truth, never training signal. Predictions are
pre-registered and committed before first runs; failed predictions are
reported as findings, not reworked.

## Artifacts

Current run artifacts live in `out/<process>/` (model.pt, config.json,
figures, logs — tracked in git). `cache.npz` is gitignored; in a clean
checkout regenerate it with
`python3 train.py --process <p> --outdir out/<p> --cache-only`, which loads
the tracked model.pt and rebuilds the cache on CPU without touching the
checkpoint (deterministic; caches recorded from MPS runs match to float
tolerance — the tracked `out/exp*.txt` logs are the canonical numbers).
Per-experiment text outputs: `out/exp<N>_<process>.txt`, tracked.
Superseded runs and rejected code live in `archive/` (gitignored).
