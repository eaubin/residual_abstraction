# Experiment log — overview

One file per experiment in `experiments/`; this file is the index and the
per-experiment writeups are **canonical** — rows here are pointers, not
summaries (slimmed at exp 15; the old long-form rows are in git history).
Conceptual framework: `README.md`; working norms: `AGENTS.md`; named
named objects, verdict taxonomy, registration checklist: `FORMALISM.md`;
cross-experiment assumption ledger: `ASSUMPTIONS.md`; **the frozen diagnostic
battery (Mess3 calibration plus
Dyck and pstack transfer, adopted at exp 18, updated at exps 22 and 28):
`BATTERY.md`**. Mess3 and Dyck work are appendix/debugging from here unless
a new process exposes a specific back-check need. Experiment review
protocol: `EXPERIMENT_REVIEW_PROTOCOL.md`.

| # | file | script(s) | question | verdict | status |
|---|---|---|---|---|---|
| 1 | [1-sufficiency.md](experiments/1-sufficiency.md) | analysis.py, refine.py | does decode-completeness saturate at the belief dimension? | yes on Z1R (k\*=1); on Mess3 top-k PCA misses the belief plane | concluded |
| 2 | [2-proposal-families.md](experiments/2-proposal-families.md) | compare.py | proposal misalignment or representation curvature? | misalignment: X-whitened PLS k\*=2 matches full-residual identification | concluded |
| 3 | [3-readout-interventions.md](experiments/3-readout-interventions.md) | intervene.py | is the discovered subspace causally load-bearing at the readout? | no — correlational-not-causal; the causal channel is the unembedding pullback | concluded |
| 4 | [4-midstream-interventions.md](experiments/4-midstream-interventions.md) | midstream.py | do mid-stream interventions persist over multi-token horizons? | P6 fails: the stream is a per-position *summary*, not propagated *state* | concluded |
| 5 | [5-depth-profile.md](experiments/5-depth-profile.md) | depth.py | at which depth does the stream function as state? | state early (L1 ≈ 94% incremental), summary late; L3 negative = *state interference* | concluded |
| 6 | [6-interventional-discovery.md](experiments/6-interventional-discovery.md) | discover.py | can observable-scored CEGAR discover a low-dim causal abstraction? | yes: k\*=2 at 98.3% vs full 98.7%, oracle-free score sound to 1.5 pts — but converged to the variance plane (mimicry limit) | concluded |
| 7 | [7-dyck.md](experiments/7-dyck.md) | dyck.py | does the battery transfer to a stack process (Dyck-2)? | causal core found (4-dim, 92.6%); linear-belief calibration fails — *representation–oracle mismatch* | concluded |
| 8 | [8-adversarial-coordinates.md](experiments/8-adversarial-coordinates.md) | adversarial.py | does discovery survive adversarially ill-conditioned coordinates? | no: variance dependence exposed (miner dies, k\*=0); acceptance never false-confident | concluded |
| 9 | [9-scale-free-mining.md](experiments/9-scale-free-mining.md) | miners.py | can the proposal miner be made scale-free? | no: invariance confirmed exactly, but the invariant miner has no causal signal — correlational mining exhausted | concluded |
| 10 | [10-interventional-search.md](experiments/10-interventional-search.md) | candidates.py | does finite-candidate interventional search work? | selection works; *patch parameterization* fails — the read side is junk-amplified ×κ | concluded |
| 11 | [11-patch-parameterization.md](experiments/11-patch-parameterization.md) | patches.py | does a (write, read) menu {id, prec, cov} fix the patch? | read *concept* vindicated (+51.3% clean vs +1.0% id, same write); the menu is insufficient | concluded |
| 12 | [12-read-search.md](experiments/12-read-search.md) | reads.py | does some spectral read c ∝ Σ̂^{−α}w suffice? | no — flat ~+1.5% across the grid; D2 composition exonerated at 97.8% | concluded |
| 13 | [13-read-gradient.md](experiments/13-read-gradient.md) | readopt.py | can the read be learned from behavioral signal? | P4 holds at last (0.3/1.6 pts); w2's read works at +43.7% with **zero plane mass**; w1 diverges | concluded |
| 14 | [14-affine-reads.md](experiments/14-affine-reads.md) | readaffine.py | mechanism settle + affine repair + EPR diagnostic | both exp-13 hypotheses die: renorm feedback refuted (divergence = per-write landscape asymmetry); working reads score EPR ≈ 0; P4 holds again | concluded |
| 15 | [15-transport.md](experiments/15-transport.md) | transport.py | statistical control vs transported state (per-pair equivalence ρ, EPR cells, distribution shifts) | adjudicated: **position-entangled statistical control** — pooled EPR was the wrong aggregation (position-t cells above threshold: aff/id 0.85–0.93, best-α mixed 0.53–0.91); learned reads invert at unseen positions (R −0.77/−0.41) while clean improves; ρ separator validated; P4 3rd hold; both decisive instruments oracle-free → battery members | concluded |
| 16 | [16-position-transport.md](experiments/16-position-transport.md) | heldout.py | can learned reads be made position-transportable by held-out-position checkpoint selection (arm A) or mixed-position training (arm B)? judged on unseen positions {10,14,22}; small write widening; ρ/EPR evaluation-only (honesty: clean reference is T-aware) | adjudication outcome (3): **clean-plane access is special; the entanglement is intrinsic to behavioral-gradient discovery in this registered geometry** — no registered 20-step checkpoint transportable on any trajectory (w2's val gain never positive); mixed-position training *memorizes* the wider set (+27.7% at trained positions, +3.0% at interpolated); 3 of 4 near-plane writes diverge (population evidence, nearest-plane worst); P4 4th hold. Read-construction thread concluded: four repair families falsified; failure mode oracle-free detectable | concluded |
| 17 | [17-generality.md](experiments/17-generality.md) | tsweep.py | are the adversarial conclusions T-generic and threshold-robust? 5 junk draws at κ=100 (draw 0 = the historical T, reproduction tripwire) + κ ∈ {30,300} secondary; per-unit battery (CEGAR accept staircase, pool angle, id/clean/spectral contrasts, gradient phenotypes, D2); benign + adversarial eps staircases over {0.01,0.02,0.05,0.10}; m deferred (different semantic target) | **P1–P9 all hold** — fully T-generic at κ=100 (5 draws: identical contrasts, accept 0 at every eps, same M2\*Sinv write — the invariant miner's); thresholds never load-bearing (benign k\*=2 and adversarial accept 0 across the whole eps grid); P7 18/18 cells ≤1.5 pts across seven transforms. **κ arm: the gradient pathology is κ-graded — at κ=30 learned reads transport** (val +40%/+37%, ρ≈0.05, still zero plane mass) **and at κ=300 everything diverges**; exps 13–16 conclusions stand at κ=100 (draw-generic), the κ=300 probe supporting the high-κ pole — re-indexed, not reopened. Secondary expectation failed informatively (κ=30 D2 85.2% = pool geometry; κ=300 spectral +12.3%) | concluded |
| 18 | [18-horizon.md](experiments/18-horizon.md) | mstair.py | the m-staircase: do diagnostics and conclusions survive changing the completion horizon? mm ∈ {1,2,3,4} (mm ≤ 3 by exact marginalization of the m=3 chain; m=4 the only new compute, ts pinned) × regimes {benign, κ=100, κ=30, κ=300}; arm A = fixed-patch diagnostics stability, arm B = CEGAR per mm + re-learning at m ∈ {1,4}; registered as the last major Mess3 calibration (intent) | **P1–P8 all hold — the registered battery is horizon-stable over the tested regimes** (closures, ρ, transport, k\*, accept-counts flat over mm ∈ {1..4}; P2 worst obs/exact gap 0.017 over 52 cells incl. m=4; κ=30 transport +40%/+37% at every mm, and re-learning at m ∈ {1,4} finds the same read; κ=100 entanglement persists at every horizon; benign k\*=2 even at mm=1 — no semantic staircase). Run-1 ρ-reference defect (§6.1 rule 8) fixed + full re-run, on the record. **Decision: Mess3 calibration closes — freeze the battery, move process class** | concluded |
| 19 | [19-dyck-baseline.md](experiments/19-dyck-baseline.md) | dyck_baseline.py | Dyck baseline + threshold recalibration (Phase 2, Block 1): does the frozen battery produce coherent numbers on Dyck-2, and what are the process-appropriate thresholds? | **P1–P6 all hold** — battery reproduces exp 7 exactly (k\*=4, 92.6%, staircase bit-identical); obs/exact band 0.064 (Mess3 0.10 transfers); ρ separation 69×; PLS echo 0.2%; val +98.7%; eps staircase weakly decreasing. All Mess3 thresholds transfer unchanged | concluded |
| 20 | [20-dyck-matrix.md](experiments/20-dyck-matrix.md) | dyck_matrix.py | Interventional battery matrix (Phase 2, Block 2): adversarial regime (κ=100), shift-retention (member 4), prefix-balance stratification, gradient read probe | **P1–P6 all hold** — adversarially opaque (accept=0, ρ 70×), robust under registered shifts (retention ≥ 0.99 both shifts, member-4 guards passed), prefix-balance-uniform (1.9% spread), single-write rank-1 probe fails at 19.7% (P7 finding, not battery failure); P1–P6 battery gates transfer unchanged; Block 2 gates passed | concluded |
| 21 | [21-dyck-robustness.md](experiments/21-dyck-robustness.md) | dyck_robustness.py | Dyck robustness sweep (Phase 2, Block 3): horizon staircase `mm≤4`, tolerance staircases, and κ coordinate stress for the exp-19 core | **P1–P8 all hold** — obs/exact worst gap 0.073 at `mm=4`; rho bands hold across `mm=1..4`; benign eps staircase is flat (`5,4,4,3`) at every horizon; adversarial accept-counts are zero for all `κ×mm×eps` cells; scope excludes shifts, multiple junk draws, and gradient-read transport | concluded |
| 22 | [22-dyck-consolidation.md](experiments/22-dyck-consolidation.md) | — | Dyck consolidation (Phase 2, Block 4): what did exps 19-21 teach, what is promoted to `BATTERY.md`/ledger scope, and should Phase 2 close? | **Phase 2 closes** — Dyck transfers the six-member battery under the registered indices; no new battery-transfer failure type; move to the next process class | concluded |
| 23 | [23-oracle-substrate.md](experiments/23-oracle-substrate.md) | scripts/oracle_withdrawal/substrate.py | Oracle-withdrawal 1: is the `pstack` measurement substrate competent, affordable, sampled-calibrated, stratified, and non-vacuous enough for hidden-oracle reference selection? | **GO** — all substrate gates pass; exact audit cheap, sampled scoring usable at 1024, strata covered, full-patch target non-vacuous | concluded |
| 24 | [24-hidden-reference.md](experiments/24-hidden-reference.md) | scripts/oracle_withdrawal/reference_selection.py | Oracle-withdrawal 2: can observable diagnostics select a compact trusted reference on `pstack` before exact audit is revealed? | **P2 fails informatively → `REFERENCE_AMBIGUITY_CONFIRMED`, NO-GO** — observable selection ties among exact-audit-indifferent compact `k=4` candidates; no unique earned reference. Subspace criterion (F2) load-bearing. Auditable follow-up = downstream-ρ (exp 25). (Revised by exp 25: the apparent distinctness is seed-fragile — see writeup.) | concluded |
| 25 | [25-rho-invariance.md](experiments/25-rho-invariance.md) | scripts/oracle_withdrawal/rho_invariance.py | Oracle-withdrawal 3: does the exp-24 reference ambiguity matter downstream for ρ? Arm A seed-stability gate on the `{pca\|cegar,delta}` split (8 seeds), then Arm B ρ anchor-invariance (does the equivalence verdict depend on whether `pca` or `cegar` anchors ρ?) | **`SEED_UNSTABLE_CLUSTER`, NO-GO** — the exp-24 two-reference split is sampling noise (clean outlier 3/8 seeds); `pstack` has one stable ~k=4 reference, so the ambiguity is moot. Recontextualizes exp 24 → exp 26. | concluded |
| 26 | [26-reference-consolidation.md](experiments/26-reference-consolidation.md) | scripts/oracle_withdrawal/reference_consolidation.py | Oracle-withdrawal 4: pre-Block-3 consolidation on `pstack` (8 fresh seeds) — is `cegar`≈`pca` (mimicry) with proper multi-seed statistics, and does ρ separate the extreme known cases under the single `cegar` anchor (validating ρ's separation, not the Dyck-inherited threshold values), with the reference estimates exact-coherent? Splits `RHO_OVERSENSITIVE` (NO-GO) from `BANDS_RECALIBRATE` (GO) | **GO to Block 3** — one coherent stable reference over 8 fresh seeds; ρ separates the extremes (`BANDS_TRANSFER`); `cegar`–`pca` `PARTIAL_MIMICRY` (~10°) but behaviorally inert at ρ, so the honest object is one behavioral reference. ρ band *values* stay Dyck-inherited. → exp 27. (Revised by exp 27: the 0.25 equivalent band is lenient at intermediate strength.) | concluded |
| 27 | [27-battery-transfer.md](experiments/27-battery-transfer.md) | scripts/oracle_withdrawal/battery_transfer.py | Oracle-withdrawal 5 (Block 3): does the six-member battery transfer to `pstack` under the **declared** `cegar` reference (4 fresh seeds; declared by convention, not uniquely earned — selection was non-unique, exps 24/25), with per-process threshold recalibration and the inherited intermediate-probe (`emb`) leniency / shift / obs-exact checks? Honest claim is system-level (the hidden-oracle workflow yields a usable battery), not new physics | **`BATTERY_TRANSFERS_WITH_RECALIBRATION(M2_rho)`, GO** — six members function under the declared `cegar` reference (M1/M3/M4/M5/M6 clean). Live finding: M2 equivalent ρ band lenient (recalibrate `pstack` 0.25→≈0.10). System-level claim; near-mimicry substrate. → consolidation (unit 6). | concluded |
| 28 | [28-consolidation.md](experiments/28-consolidation.md) | — | Oracle-withdrawal 6: consolidate the reference arc (23–27) — promote `pstack` indices + new typed findings to `BATTERY.md`, settle the ledger, decide program disposition | **Reference arc closes, mixed typed result** — oracle-free unique selection FAILED (non-unique; declared anchor), but the workflow TRANSFERRED a usable battery under it (exp 27). Both halves; do **not** round up to "earned." Promoted to `BATTERY.md`; recommend close (units 4/5 optional on near-mimicry pstack). | concluded |
| 29 | [29-predicate-targeting.md](experiments/29-predicate-targeting.md) | scripts/predicates/predicate_targeting.py (+ `predicates.py`) | `ORIGINAL_SIN` reduction #3 measurement pilot: are within-horizon predicate targets non-vacuous/interpretable on `pstack`, and do registered affine readout directions provide routing evidence relative to PCA/core? No predicate CEGAR or composition claim; first build under the library-home rule | **Readout/control split** — two registered predicates decode and pass exact endpoint audit, but their registered rank-1 writes do not causally move predicate probability; one predicate is flat and one is not recovered by tested interpreters. No geometry-routing result. | concluded |
| 30 | [30-fixed-read-oblique-write-search.md](experiments/30-fixed-read-oblique-write-search.md) | scripts/interventions/i1_fixed_read_write_search.py | I1: with exp-29-style affine predicate reads fixed on `pstack`, can separate oblique write directions control `phi1`/`phi2` on held-out positions? | **`FIXED_READ_LIMIT(phi1_next_closes,phi2_net_return)`** — fixed reads decode on discovery positions but fail held-out-position R², so I1 cannot adjudicate write failure. | concluded |
| 31 | [31-read-transport-atlas.md](experiments/31-read-transport-atlas.md) | scripts/interventions/read_transport_atlas.py | Diagnostic pre-I2: is each `pstack` predicate linearly readable **in place** at the held-out positions exp 30 only ever tested as an off-diagonal transfer cell, and if so is the read direction shared or position-specific? Observable-only, no patch. | **`ATLAS(phi1_next_closes=POSITION_SPECIFIC_READ, phi2_net_return=POSITION_SPECIFIC_READ)`** (4/4 seeds, positions exchangeable) — both predicates *are* linearly readable in place at held-out positions at `R2` comparable to discovery, but the read direction is position-specific (near-orthogonal covectors; no single-position read carries). Exp 30's negative is a transport-of-read failure, not representational absence. Readability claim only; routes to I2 with position-conditioned reads. | concluded |
| 32 | [32-read-transport-discriminator.md](experiments/32-read-transport-discriminator.md) | scripts/interventions/read_transport_discriminator.py | Pre-I2 gate: is exp 31's held-out read failure a genuinely position-specific read direction, or one shared direction needing only per-position gain/bias recalibration? Refit-primary discriminator (freeze disc direction, recalibrate gain/bias at held); within-position cosine-reliability ceiling vs `1/√d` is a reported overlay only. Observable-only, no patch. | **`GATE(phi1_next_closes=POSITION_SPECIFIC_CONFIRMED, phi2_net_return=POSITION_SPECIFIC_CONFIRMED)`** (4/4 seeds each) — freezing the disc direction and refitting only gain/bias recovers held `R2` ≤ ~0.36 against an in-place ceiling ~0.55–0.75 (`recovers=False`), so the held read is a genuinely different direction, not scale drift. `cosine_instrument` overlay `UNRELIABLE` (ceiling 0.24–0.35 < 0.50): exp-31's near-zero cross cosine was an unusable sharing signal, vindicating the refit-primary amendment. Routes to I2 with position-conditioned reads; pooled cross-check shows a milder shared subspace is not excluded. Readability claim only, no patch. | concluded |
| 33 | [33-poscond-read-write-search.md](experiments/33-poscond-read-write-search.md) | scripts/interventions/i1prime_poscond_read_write_search.py (+ `intervention_eval.py`) | I1′: with the read repaired to a position-conditioned in-place fit per bin (exp 31/32), can a separate oblique **write** control `phi1`/`phi2` on held-out positions — the I1 write question exp 30 stalled before? The explicit fixed-read baseline the §I2 Pre-I2 block requires. | **`NO_POSCOND_READ_WRITE_WORKS(phi1_next_closes,phi2_net_return)`** — repaired in-place reads decode with room, but the registered fixed-read rank-1 oblique write menu does not stably control either predicate; fixed-read baseline is a clean negative. | concluded |
| 34 | [34-matched-delta-gate.md](experiments/34-matched-delta-gate.md) | scripts/interventions/i3_matched_delta_gate.py | I3-lite: before an optimizer-heavy learned read/write-pair search, test whether same-position, same-sign, target-marginal-matched observed activation deltas can move `phi1`/`phi2` on held-out positions at `alpha<=1`, with specificity and finite full-m-gram audit. | **`NONSPECIFIC_DELTA(phi1_next_closes,phi2_net_return)`** (4/4 seeds, both targets) — matched deltas move each predicate (held control 0.63–0.78, beating mismatched/shuffled floors, with transfer; `own_delta` ceiling closes to 1.00) but co-move the other predicates (specificity 0.56–0.68 ≫ 0.35); partly extrapolated for `phi2`. Near-manifold move exists but is entangled, not predicate-specific. | concluded |
| 35 | [35-joint-state-separability.md](experiments/35-joint-state-separability.md) | scripts/interventions/i3b_joint_state_separability.py | 2b: re-adjudicate the exp-34 near-manifold deltas (no new optimizer) as joint stack-state control (`phi1`+`phi2` move together) vs out-of-bundle specificity (`phi4` spared + bounded m-gram), to decide whether exp-34's non-specificity was correct identification of one variable (`JOINT_STACK_VARIABLE`), genuine broad replacement (`BROAD_STATE_REPLACEMENT`), or truly separable predicates (`SEPARABLE_PREDICATES`). | **`BROAD_STATE_REPLACEMENT(stack_state_bundle)`** (4/4 seeds, both arms) — the bundle *is* directionally coupled (`bundle_co` 0.63–0.72 beats no-info floors by `C_MARGIN`), and the move is partial (m-gram ~0.50 vs `own_delta` 1.00), but it also drags out-of-bundle `phi4` above the sparing ceiling (`\|c(phi4)\|` 0.36–0.62 > 0.35) → registered branch `BROAD`. **The robust claim is narrower:** the discriminating `phi4` axis is underpowered (room 0.025–0.037, flagged 4/4) **and** possibly biased (`phi4` may be partly stack-coupled), so the move is only *not demonstrably bundle-specific* — "broad replacement" is the label, not an established mechanism. Net: `pstack` lacks a control both high-room and known out-of-bundle; routes to the exit gate (richer toy) over another `pstack` residual probe. Not clean joint control. | concluded |
| 36 | [36-icb-consolidation.md](experiments/36-icb-consolidation.md) | — (consolidation, like exps 22/28) | Phase 3 (intervention-class benchmark) consolidation: what did exps 29–35 settle about intervention classes on `pstack`, what carries forward, and is the exit gate decidable? | **Phase 3 closes — a typed substrate-adequacy verdict.** No intervention class earned a specific, transferable positive on `pstack` predicates, because the toy cannot decide specificity for its **coupled stack-state** targets (no high-room, known-out-of-bundle control). Carry-forwards: (1) matched-delta / interchange interventions outrank rank-1 linear oblique writes for these targets (movement only — specificity never adjudicable); (2) predicate reads are **position-specific** (exps 31–32), so fix a position-conditioned read. The `phi1`/`phi2` **coupling** is a flagged open finding (valence uncertain — one stack-state variable vs broad replacement) handed to the next toy. Decision: **exit gate** (backward-designed richer toy with a separable, high-room, absolute-marginal-scored out-of-bundle control), **not** I2/I4 on `pstack`. | concluded |
| 37 | [37-localization-substrate-gate.md](experiments/37-localization-substrate-gate.md) | scripts/localization/l0_substrate_gate.py | State-localization phase, **L0**: build + self-test the interchange harness and run a per-facet GO/NO-GO substrate gate on Dyck-2 {`depth`, `top_type`} — non-vacuous, estimator-vs-oracle calibrated, clean/source separated, observably pure (floor = within-class spread ÷ gap), dissociable per (position × held-value) cell. All estimators forced to **m=1** (the position-`t` patch transports only the next-token prediction — the exp-4/5 summary wall, recurring), so `depth` is the **close-readiness** proxy, not graded depth. No localization claim (L1's). | **`top_type → GO`, `depth → FLOOR_FAIL` (HELD); 4/4 seeds.** **P3 (the real unknown) resolves favorably:** dissociable single-facet pairs are abundant — every (position × held) cell clears 256 with room (510–512 pairs), 12 type / 8 depth cells, no `NOT_DISSOCIABLE`. `top_type` clean on every axis (`delta` 1.000, floor 0.000, oe 0.000) → certified L1 substrate. `depth` close-readiness well-separated and boundary-carried (~2750 boundary vs ~90 interior) but **floor 0.051–0.054** — just *above* `NULL_TOL=0.05`, a **non-marginal** FLOOR_FAIL (P2 verdict holds; its "marginal" sub-class does not). Read as **purity uncertified, not impure** (within-class spread ≈5% of gap, near the clean end; impurity vs model-approximation unseparated at L0) → carried to L1 flagged, the random-unit floor is the real baseline. No facet dropped, no redesign. | concluded |
| 38 | [38-l1-propagation-gate.md](experiments/38-l1-propagation-gate.md) | scripts/localization/exp38_propagation_gate.py (+ `localize.py` core; `exp38_ceiling_smoke.py` feasibility) | State-localization **L1**: propagating-state instrument (exact teacher-forced m≥2 forced-close conditional, no rollout) + a propagation gate. Discriminator = sufficiency/locality curve + necessity + depth-over-horizon, vs matched random-placement/random-drop controls. Negative is claim-bounded ("not localizably summarized", never "not carried" — redundancy not excludable). | **`DISTRIBUTED` at `k=1` and `k=2`, stable 4/4 seeds.** Graded depth **is carried/transportable** (full-/large-window patch transports 0.83–0.97 of the oracle gap, ≈0 same-depth floor → not `RECOMPUTED`) **but not point-localized** (contiguous transport ramps gradually, beats matched random placement on average → recency-weighted, but no small window saturates — the binding reason; necessity of `t` largely *passes*, which is a recency signature, not a locus). Updates exp-4/5 "summary not state" *for graded depth*: carried state, distributed not point-localized. Planted-locus transfer-validity gate GREEN. Claim-bounded: not "recomputes" (carriability refutes), not a spread-vs-redundancy mechanism claim. Routes L2 to read graded depth as distributed (no single-locus expectation); recency ramp feeds the dynamics rung. | concluded |
| 39 | — *(gap — intentionally skipped)* | — | The L2 coarse-localization design (block-granularity importance × specificity, same-vs-different-parts) was **deferred behind exp 40** and pulled out of the numbered ledger; see [l2-coarse-localization.md](experiments/l2-coarse-localization.md) (un-numbered). A revived L2 takes the next free number. | gap |
| 40 | [40-directional-specificity.md](experiments/40-directional-specificity.md) | scripts/localization/exp40_directional_specificity.py (`localize.py` steering core) | State-localization **interventional dissociation** rung (supersedes L2 as next step). Isolates the **directional** axis 38 left open (38 settled only the *spatial* axis: graded depth is spread across positions): at fixed full spatial support, are the depth- and `top_type`-carrying residual directions **separable**? Construct = rank-1-per-position **additive diff-in-means steering vectors** from observable-matched pairs; the 2×2 **dissociation matrix** (target transport × cross-drag), read at **matched target-transport** over an α-sweep vs a random-direction floor and the full-replacement ceiling. Full replacement is tautological for specificity (37 transport guard), so the steer must be sub-residual/additive. | **`SEED_UNSTABLE`** (registered precedence; `out/exp40_directional_specificity.txt`, 4 seeds) — driven by a borderline `k=1`. Substance = **asymmetric `CROSS_DRAG`**, stable at `k=2` (4/4): the **`top_type` direction is `SPECIFIC`** (drag on depth 0.01–0.05), but the **depth direction drags `top_type`** (k=1 0.11–0.20 ≈ bound 0.15 → unstable; k=2 0.20–0.41 ≫ bound), **larger at the deeper (k=2) contrast**. **Both facets have a rank-1-per-position additive handle** (depth target reaches/overshoots ceiling — refines 38's spatial `DISTRIBUTED`: distributed-across-positions ≠ no per-position low-rank direction). Bound: about the matched-pair difference-direction at full support, not a causal sum→ratio law (`v_depth` non-orthogonal to the type direction — `(q2,q3)` purity partially live). Routes to characterizing the binding (type-orthogonal depth direction?); an analogous coupling on a separable-by-construction toy, consistent with the exp-36/pstack coupling (ledger row 37) being representational (different process, not a direct resolution). **[exp 42 characterized this drag as curvature/geometric — representational reading revised; see row 42.]** | concluded |
| 41 | [41-product-counter-substrate.md](experiments/41-product-counter-substrate.md) | scripts/product_counter/derive_thresholds.py; scripts/product_counter/substrate.py | Product-counter planted-carrier discriminator from the exp-36 exit constraint: substrate/carrier guards plus contextual exact, value-mean, observable-min-norm, and random-floor arms. | **NO CLAIM — PROCEDURAL FAILURE / QUARANTINED PILOT.** The review pause was missed and route-bearing behavior was exposed during repair; exp 41 cannot count as preregistered evidence. The script is retained only as a compact pilot harness for a future clean experiment. | procedural failure |
| 42 | [42-readout-mechanism.md](experiments/42-readout-mechanism.md) | scripts/localization/exp42_readout_mechanism.py (+ non-claim pre-checks: exp42_geometry_precheck.py; the seed-700 design run) | State-localization **directional-binding characterization** rung — the route out of exp 40's `CROSS_DRAG`. Is the depth→`top_type` drag **geometric** (curvature of a depth-INDEPENDENT type readout, vanishing as α→0) or **representational** (a depth-CONDITIONAL readout whose direction rotates with depth)? **Redesigned from the guarded-specificity draft** (superseded — two pre-checks showed a single-point guard cannot separate the mechanisms: it is fit at one operating point while the steer moves off it, and the type readout is rank-1/~orthogonal to `v_depth`). Construct = **direct measurement**: readout rotation `cos(g_lo,g_hi)` of the finite-difference `top_type`-readout gradient across depth (split-half reliability-gated; **no autodiff**) + drag slope `p` vs α over the rising prefix (first-order vs curvature). Reports the `(k1,k2)` **configuration** (the `OUTCOME_STRUCTURE` lens). Registered prediction `{k1: GEOMETRIC, k2: CURVATURE_W_ROTATION}` frozen at `d20363e`. | **`{k1: GEOMETRIC, k2: CURVATURE_W_ROTATION}`** (matches the registered prediction; 4 seeds, `out/exp42_readout_mechanism.txt`) — the drag is **curvature-dominated / separable to first order** (linear limit, rank-1 additive class): shallow contrast curvature with a depth-independent readout (k1 4/4, `cos`≈0.95, slope `p`≈4–6); the exp-40 large-α drag is reinterpreted as curvature, not eliminated. The larger deep-contrast gradient rotation (k2 3/4; `cos`≈0.85; seed 702 `NO_DRAG`) is **consistent with that same readout nonlinearity and is NOT distinguished from a representational depth-conditional readout** (result-review finding: split-half `rel` bounds within-cloud variation only) — its representational reading is withdrawn. **No representational binding is established** anywhere; resolves exp 40's confound toward **geometric/curvature** and motivates the deferred **L2** (localize/intervene each facet). A clean representational test would need a same-depth matched-displacement / gating control this design lacks. | concluded |

## Conventions

KL in nats, held-out unless stated; "affine R²" = held-out R² of an affine
map fit on the train split. Honesty constraint everywhere: proposal families
and probes are supervised on observables (completions) only — belief states
are evaluation ground truth, never training signal. Predictions are
pre-registered and committed before first runs; failed predictions are
reported as findings, not reworked.

**Redundancy policy (adopted at exp 15).** The per-experiment writeup is
the single canonical record of a conclusion. Everything else points at
it: index rows above are one line; script docstring RESULTS sections are
≤ 3 lines plus the pointer; `ASSUMPTIONS.md` ledger rows carry only the
*current status* of an assumption. Conclusions are written once, not four
times.

**Code policy (adopted at exp 15).** Concluded experiment scripts are
frozen records — they keep their inline machinery and are never
refactored. The living edge (exp 15 onward) imports the shared
scaffolding from `expcommon.py` (guards, gate, anchor, transform, write
reproduction, differentiable chain, affine optimizer, shared metrics), so
there is exactly one live copy; new scripts contain only
experiment-specific content. Registrations reference "the standard
setting" and "the standard guards/self-checks" instead of restating them,
and state only deltas.

## Artifacts

Current run artifacts live in `out/<process>/`. Text logs, configs,
figures, and per-experiment outputs are tracked when they are part of the
record; `cache.npz` is gitignored. New `model.pt` checkpoints are also
gitignored by default and should be regenerated from the registered
training command unless a registration/review explicitly marks that run
non-reproducible without the checkpoint. Historical tracked checkpoints
remain as records. Per-experiment text outputs:
`out/exp<N>_<process>.txt`, tracked.
Superseded runs and rejected code live in `archive/` (gitignored).
