# Experiment 10 — finite-candidate interventional search — PRE-REGISTRATION

**Script:** `candidates.py`. **Status: pre-registered; NOT YET RUN —
results to be appended below the marked line.**

**Question.** Experiment 9 exhausted correlational proposal mining: the
covariance miner is variance-lucky, the invariant miner removes the luck
and with it the only selection signal, decorrelation dies adversarially.
The acceptance rule has been the sole causally-grounded component all
along. This experiment makes *selection itself interventional*: candidate
directions are generated by several cheap (deliberately fallible) sources
— including the Experiment-9 losers — and ranked by **measured closure
gain under actual patching**, with the acceptance rule unchanged. Per the
external review that shaped this design: gradient-based proposal
generation is a different machine (objective smoothness, parameterization,
off-manifold exploitation) and is **deferred to Experiment 11**, contingent
on this experiment; it does not appear here even as an exploratory family.

**The trichotomy this experiment adjudicates** (registered interpretive
rules, applied to the diagnostics below):

1. *Selection works from a broad pool* — adversarial success with accepted
   directions drawn from coordinate-robust sources: proposal generation is
   solved in principle (generation can be cheap and broad; selection is
   causal).
2. *Support failure vs selection failure* — adversarial failure is
   diagnosed by the per-candidate table: if no candidate in any round had
   both a small angle to the causal plane and a gain above threshold, the
   *pool* lacked support (generation still unsolved); if near-plane
   candidates existed but their measured gains were small, the *patch
   parameterization or acceptance objective* is insufficient — a deeper
   problem than generation.
3. *Mere reranking* — benign success that only ever accepts
   variance-lucky sources (M1/M3/delta-PCA), with adversarial failure:
   interventional selection added nothing over Experiment 6.

## Design

**Setting.** The Experiment-6/8/9 setting unchanged (`out/mess3-L4`,
patch L1, prefix-wide, κ = 100, eps_gain 0.05, eps_drop 0.01, k_max 8,
400/600 disjoint discovery/evaluation pairs, basis sample 800, m = 3,
seed 0 registered, anchor = the frozen Experiment-6 covariance loop with
its asserted fixed point, T and transform checks as in Experiments 8–9).

**The search loop.** The CEGAR acceptance rule is unchanged; only the
proposal step changes: each round, generate a candidate pool (below), patch
each candidate jointly with the current accepted basis, measure its actual
closure gain on the discovery pairs (one chain evaluation per candidate —
the scores are exact given the pair set, no estimation noise), accept the
best candidate iff its gain ≥ eps_gain, else stop. Coarsen pass unchanged.
Selection-on-discovery overfitting is bounded by the disjoint evaluation
pairs, on which all verdicts are computed.

**Patch convention (the coordinate-luck control, per review).** Candidates
are unit directions in the *working* coordinates of the regime (x benign,
z adversarial), orthogonalized against the accepted basis; the patch is
the plain orthogonal-coordinate swap in working coordinates, **pulled back
to stream space** before touching the model (the Experiment-8 pullback).
Success is always judged after pullback. Note this differs from
Experiment 9's M2, whose whitened-swap patch had weak oblique coupling:
here every candidate, whatever its source geometry, is patched by the same
working-coordinate convention — so a near-plane direction found by the
whitened miner gets a fair interventional test it never got in
Experiment 9.

**The candidate pool (registered: up to 12 per round — candidates whose
residualization against the accepted basis is numerically null are
dropped, and the surviving count is printed per round so the selection
pressure stays auditable; review clarification).** All computed from the
working-coordinate data the regime exposes; per-round randomness from
`default_rng(seed + 1000 + round)`:

| source | count | note |
|---|---|---|
| M1 (covariance miner) | 1 | variance-lucky; junk-poisoned adversarially |
| M3 (centered-weight miner) | 1 | same |
| M2 miner, both mappings Σ̂^{1/2}u and Σ̂^{-1/2}u | 2 | the exp-9 loser, now patched by the uniform convention; both back-mappings included because the right one is not knowable a priori |
| delta-PCA (unweighted top-2, residualized) | 2 | pure variance control |
| random isotropic | 2 | no-information |
| random covariance-shaped (Σ̂^{1/2}g) | 2 | data-distributed random |
| random precision-shaped (Σ̂^{-1/2}g) | 2 | whitened random |

**Registered diagnostics (no thresholds; these adjudicate the
trichotomy).** Per round, per candidate: source, measured gain, and the
angle of its pulled-back stream-space direction to the causal plane
(evaluation-side use of the anchor plane — itself observable-derived).
Source attribution of every accepted direction. These tables are the
output's load-bearing content even if every prediction fails.

## Pre-registered predictions

- **P1 (anchors).** The frozen Experiment-6 loop reproduces (k\* = 2,
  c_obs ≈ 99.8%); transform checks pass.
- **P2 (benign).** The interventional search converges at k\* ≤ 4 with
  exact-target closure ≥ 90% of full.
- **P3 (adversarial — the headline; credence ~60%, registered as such).**
  Same bars in the adversarial regime, judged after pullback: k\* ≤ 4,
  exact closure ≥ 90% of full.
- **P4 (observable soundness, three-way as established).** |c_obs − exact|
  ≤ 0.10 on a **non-null** accepted adversarial patch; NOT TESTED if
  k\* = 0.
- **P5 (plane containment, adversarial).** Both causal-plane directions
  lie within 15° of the discovered subspace, **which requires k\* ≥ 2**
  (pre-run clarification from review: with k\* = 1 only one principal
  angle exists and a 1-D subspace cannot contain the 2-D plane — the
  dimension-parity loophole closed in Experiment 8's P3, recurring here in
  containment form). Containment form is used because the search may
  accept k\* > 2 slightly-blurred directions; dimension parity and
  projection distance reported as characterization.
- **P6 (validity gate, enforced).** As in Experiments 5–9.

## Failure modes this can newly exhibit

*Pool-support failure* — no round's pool contains a candidate with both
near-plane angle and acceptable gain (trichotomy outcome 2a): cheap
generation cannot reach the causal plane in hostile coordinates at this
pool size; the honest next step is larger/adaptive pools or gradients
(Experiment 11), not threshold-tuning. *Patch-parameterization failure* —
near-plane candidates exist but earn no gain (outcome 2b): the working-
coordinate orthogonal swap is the wrong patch family; would implicate the
interchange construction itself, the deepest revision. *Selection
overfitting* — discovery-pair closure high, evaluation closure low: the
12-candidates × rounds selection pressure exceeded what 400 pairs support;
diagnosed by the c_obs-vs-exact gap that P4 measures.

**Self-checks** (every invocation; `--selftest` exits after the standard
four): the standard four; anchor and transform checks (real runs); loop
invariants (candidates unit-norm and orthogonal to the accepted basis in
working coordinates; D₀ > D_full; c_obs(full) = 1).

**Enforcement.** Registered parameters, full model config, seed 0, and the
gate guarded as in Experiments 8–9 (`--force-invalid` demotes to a labeled
exploratory run).

---

**Results to be appended below this line after the first run.**
